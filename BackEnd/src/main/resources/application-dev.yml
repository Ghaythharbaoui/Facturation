spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/facturedb
    username: postgres
    password: ghayth
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

app:
  jwt:
    secret: ${JWT_SECRET:bXktc3VwZXItc2VjcmV0LWtleS1mb3Itand0LXRva2Vucy1taW4tMzItY2hhcnM=}
    expiration: ${JWT_EXPIRATION:86400000}
  upload:
    dir: ${UPLOAD_DIR:./uploads}

logging:
  level:
    com.facturestock: DEBUG
    org.springframework.security: DEBUG
